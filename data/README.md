# Data

Local datasets and generated Parquet files belong here and are ignored by Git. Expected filenames
and fields are documented in the [dataset contracts](../docs/en/datasets.md).

Document each dataset's source, license, schema, and generation command before adding it to a
workflow. Do not commit confidential or production data.
